import {savePageSettings} from '~/server/services/page-settings';import {useDb} from '~/server/db';import {pageSettingsInput,pageSettingsKeys} from '~/server/utils/validators'
export default defineEventHandler(async(e)=>{const page=String(getRouterParam(e,'page')||'');if(!pageSettingsKeys.includes(page as any))throw createError({statusCode:404,statusMessage:'Page settings tidak ditemukan.'});const b=await readValidatedBody(e,pageSettingsInput.safeParse);if(!b.success)throw createError({statusCode:400,statusMessage:b.error.issues[0]?.message||'Pengaturan tidak valid.'});try{return {data:await savePageSettings(useDb(),page,b.data)}}catch(err){throw createError({statusCode:400,statusMessage:err instanceof Error?err.message:'Pengaturan tidak valid.'})}})
