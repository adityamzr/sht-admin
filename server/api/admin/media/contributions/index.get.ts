import {listContributions,countContributions} from '~/server/services/contributions';import {adminContribution} from '~/server/utils/serializers';import {useDb} from '~/server/db'
export default defineEventHandler(async(e)=>{const q=getQuery(e),page=Math.max(1,Number(q.page)||1),pageSize=Math.min(50,Math.max(1,Number(q.pageSize)||12));const f={search:typeof q.search==='string'?q.search.trim():undefined,status:typeof q.status==='string'?q.status:undefined,type:typeof q.type==='string'?q.type:undefined,city:typeof q.city==='string'?q.city:undefined};const db=useDb();const [rows,total]=await Promise.all([listContributions(db,{...f,limit:pageSize,offset:(page-1)*pageSize}),countContributions(db,f)]);return {data:rows.map(adminContribution),meta:{page,pageSize,total,pageCount:Math.ceil(total/pageSize)}}})
