import {createContribution} from '~/server/services/contributions';import {publicContribution} from '~/server/utils/serializers';import {publicContributionInput} from '~/server/utils/validators';import {notifyMediaAdmins} from '~/server/services/notifications';import {useDb} from '~/server/db'
export default defineEventHandler(async(e)=>{const b=await readValidatedBody(e,publicContributionInput.safeParse);if(!b.success)throw createError({statusCode:400,statusMessage:b.error.issues[0]?.message??'Kontribusi tidak valid.'});const d=b.data;const clean=(v:string|null|undefined)=>v||null;const r=await createContribution(useDb(),{...d,city:clean(d.city),subject:clean(d.subject),name:clean(d.name),contact:clean(d.contact),sourcePage:clean(d.sourcePage),sourceUrl:clean(d.sourceUrl),mapsUrl:clean(d.mapsUrl)});await notifyMediaAdmins(useDb(),{type:'CONTRIBUTION_NEW',title:'Kontribusi baru',message:`${d.type} dikirim dari ${d.city||'Umum'}.`,href:'/media/contributions'});return {data:publicContribution(r)}})
